export type DateStr = `${number}-${number}-${number}`;
export type TimeStr = `${number}:${number}:${number}`;
export type Timestamp = number;
export type JsonStr = string;
export type QuotedVal = string;
