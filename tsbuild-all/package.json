{
  "name": "@vlcn.io/typed-sql-tsbuild-all",
  "version": "0.0.1",
  "main": "lib/index.js",
  "type": "module",
  "private": "true",
  "devDependencies": {
    "typescript": "^5.0.4"
  },
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build",
    "watch": "tsc --build -w",
    "deep-clean": "rm -rf ./lib || true && rm tsconfig.tsbuildinfo || true"
  }
}
